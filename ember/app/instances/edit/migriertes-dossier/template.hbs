<CamacForm @instance={{@model.instance}} @meta={{@model.meta}} as |form|>
  <form.input @identifier="kommunale-gesuchsnummer"/>
  <form.input @identifier="kantonale-gesuchsnummer"/>
  <form.input @identifier="betroffene-nutzungszonen"/>
  <form.input @identifier="verfahrensart-migriertes-dossier"/>
  <form.input @identifier="freies-textfeld-1"/>
  <form.input @identifier="freies-textfeld-2"/>
  <form.input @identifier="link"/>
</CamacForm>