<CamacForm @instance={{@model.instance}} @meta={{@model.meta}} as |form|>
  {{form.table "projektverfasser-planer"}}
  {{#if this.questionActive}}
    <UkButton
      @on-click={{fn this.copyQuestionValue}}
      class="uk-align-center uk-margin-remove-vertical"
      @disabled={{unless this.bauherrschaftValue "true"}}
    >
      Bauherrschafts Personalien Übernehmen
    </UkButton>
  {{/if}}
</CamacForm>