<CamacForm @instance={{@model.instance}} @meta={{@model.meta}} as |form|>
  <form.input @identifier="ausnahmen"/>
  <form.input @identifier="ausnahmen-andere"/>
  <form.input @identifier="ausnahmen-begrundung"/>
</CamacForm>