{{#power-select-multiple
  selected      = model.value
  options       = config.options
  searchEnabled = (not (not config.searchable))
  disabled      = readonly
  onchange      = (action (optional on-change))
as |option|}}
  {{option}}
{{/power-select-multiple}}
