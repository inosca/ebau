{{yield (hash
  input = (component 'camac-input' instance=instance readonly=readonly)
  table = (component 'camac-table' instance=instance readonly=readonly)
  gwr   = (component 'camac-gwr' instance=instance readonly=readonly)
)}}
