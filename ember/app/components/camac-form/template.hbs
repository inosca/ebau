{{yield (hash
  input = (component 'camac-input' instance=instance readonly=(not (contains 'form' meta.editable)))
  table = (component 'camac-table' instance=instance readonly=(not (contains 'form' meta.editable)))
  gwr   = (component 'camac-gwr' instance=instance readonly=(not (contains 'form' meta.editable)))
)}}
