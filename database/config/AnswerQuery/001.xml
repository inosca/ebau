<row>
  <table_name>answer_query</table_name>
  <model_name>configuration.AnswerQuery</model_name>
  <data>
    <answer_query_id type="int">1</answer_query_id>
    <name type="str">Gemeinde</name>
    <query type="str">SELECT
  LOCATION_ID, NAME
FROM
  LOCATION
  
WHERE
  LOCATION_ID IN (
    SELECT
      LOCATION_ID
    FROM
      GROUP_LOCATION
    WHERE
      GROUP_ID = [GROUP_ID]
    )
    OR
    [ROLE_ID] NOT IN (6, 7, 1105)
ORDER BY NAME</query>
  </data>
</row>

