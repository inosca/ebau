<row>
  <table_name>A_EMAIL</table_name>
  <model_name>configuration.AEmail</model_name>
  <data>
    <ACTION_ID type="int">251</ACTION_ID>
    <SENDER_NAME type="str">Camac Info</SENDER_NAME>
    <SENDER_EMAIL type="str">camac-info@ur.ch</SENDER_EMAIL>
    <QUERY type="str">SELECT
  "USER"."EMAIL",
  "USER"."NAME" || ' ' || "SURNAME" "NAME"

FROM "USER"

LEFT JOIN "USER_GROUP" ON (
  "USER_GROUP"."USER_ID" = "USER"."USER_ID"
  AND
  "USER_GROUP"."DEFAULT_GROUP" = 1
)

LEFT JOIN "GROUP_LOCATION" ON (
  "GROUP_LOCATION"."GROUP_ID" = "USER_GROUP"."GROUP_ID"
)

WHERE "GROUP_LOCATION"."LOCATION_ID" = [INSTANCE_LOCATION_ID]
AND
"USER"."EMAIL" IS NOT NULL
and
"USER"."DISABLED" = 0</QUERY>
    <TITLE type="str">Dossier an Gemeinde zurück geschickt</TITLE>
    <TEXT type="str">CAMAC Info (das ist eine automatisch generierte E-Mail von Camac, bitte nicht darauf antworten)

Rückmeldung an Gemeinde, Dossier abgeschlossen
Die Koordinationsstelle für Baueingaben hat das Dossier Nr. [@c2q6i1] im CAMAC abgeschlossen. Die Originaldokumente der Fachstellen und die Gesuchsunterlagen erhalten Sie in den nächsten Tagen per Post.

BITTE VERGESSEN SIE NICHT, DAS DOSSIER IM CAMAC ABZUSCHLIESSEN
(Upload Protokolle, Entscheiddokumente, Stellungnahme, Endabnahmeprotokoll usw., Dossier archivieren).

Bei Fragen stehen wir Ihnen gerne zur Verfügung.

Amt für Raumentwicklung
Koordinationsstelle für Baueingaben
Rathausplatz 5
6460 Altdorf
Tel. 041 875 24 24
Fax: 041 875 24 25
E-Mail: paul.walker@ur.ch</TEXT>
  </data>
</row>

