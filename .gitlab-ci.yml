before_script:
  - source /etc/profile
  - pyenv local 3.5.1
  - export PIP_CACHE_DIR='pip-cache'
  - pip install -r docker/requirements.txt

services:
    - oracle
    - memcached

stages:
    - test

run-test:
    stage: test
    script:
        - hostname
        - echo $PWD

