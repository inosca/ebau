mutation CreateCirculationWorkflow {
  saveWorkflow(
    input: {
      slug: "circulation"
      name: "Zirkulation"
      startTasks: ["activation"]
      allowForms: ["circulation"]
      isPublished: true
    }
  ) {
    clientMutationId
  }
}
