mutation CreateInternalWorkflow {
  saveWorkflow(
    input: {
      slug: "internal"
      name: "Internes Verfahren"
      startTasks: ["submit"]
      allowForms: ["baupolizeiliches-verfahren", "zutrittsermaechtigung"]
      isPublished: true
    }
  ) {
    clientMutationId
  }
}

mutation CreateFlow1 {
  addWorkflowFlow(
    input: {
      workflow: "internal"
      tasks: ["submit"]
      next: "['ebau-number', 'create-manual-workitems']|tasks"
    }
  ) {
    clientMutationId
  }
}

mutation CreateFlow2 {
  addWorkflowFlow(
    input: {
      workflow: "internal"
      tasks: ["ebau-number"]
      next: "['audit', 'decision']|tasks"
    }
  ) {
    clientMutationId
  }
}
