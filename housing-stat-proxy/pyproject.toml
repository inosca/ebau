[project]
name = "housing-stat-proxy"
version = "0.1.0"
description = "A simple python proxy that sits between our frontend and the housing stat API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
    "requests>=2.32.5",
]
