{{#if this.hasMoreThenOneRoute}}
  <UkTab as |Tab|>
    {{#each this.includeSubRoutes as |subRoute|}}
      <Tab.item @href={{concat "/service-permissions/" subRoute}}>
        {{t (concat "service-permissions." subRoute)}}
      </Tab.item>
    {{/each}}
  </UkTab>
{{/if}}
{{outlet}}