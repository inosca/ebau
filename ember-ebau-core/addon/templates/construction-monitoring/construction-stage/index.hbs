<div class="uk-flex uk-flex-middle uk-flex-between">
  <div class="uk-text-lead {{if (is-legacy-app) 'uk-text-bold'}}">
    {{this.constructionStage.displayName}}
  </div>
  <div
    class="uk-label uk-label-{{this.status.color}}
      uk-margin-left
      {{if (is-legacy-app) 'uk-text-bold'}}"
  >
    {{this.status.label}}
  </div>
</div>
<hr />
{{#if this.infoText}}
  <div class="uk-text-wrap uk-margin uk-margin-remove-bottom">
    {{this.infoText}}
  </div>
{{/if}}

{{#if (await (can "cancel construction-stage" this.constructionStage))}}
  <UkButton
    class="uk-margin"
    @onClick={{perform this.cancelConstructionStage}}
    @label={{t "construction-monitoring.construction-stage.cancel"}}
    @loading={{this.cancelConstructionStage.isRunning}}
    @disabled={{this.cancelConstructionStage.isRunning}}
  />
{{/if}}