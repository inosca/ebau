query GetActiveDistribution($instanceId: GenericScalar!) {
  allWorkItems(
    filter: [
      { task: "inquiry" }
      { rootCaseMetaValue: { key: "camac-instance-id", value: $instanceId } }
      { status: READY }
    ]
  ) {
    totalCount
  }
}
