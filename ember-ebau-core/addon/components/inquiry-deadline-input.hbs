<CfField::Input::Date @field={{@field}} @onSave={{@onSave}} @disabled={{this.isDisabled}} @context={{@context}} />

{{#if this._showHint}}
  <div class="uk-alert uk-alert-warning">
    <p>
      <strong>
        {{t "distribution.inquiry-deadline-input.disabled-hint"}}
      </strong>
    </p>
  </div>
{{/if}}
