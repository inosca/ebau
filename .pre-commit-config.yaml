repos:
-   repo: local
    hooks:
    - id: ruff-check-and-fix
      stages: [pre-commit]
      name: fix style and imports
      language: system
      entry: ruff check --fix
      types: [python]
      pass_filenames: true
    - id: ruff-format
      stages: [pre-commit]
      name: format code
      language: system
      entry: ruff format
      types: [python]
      pass_filenames: true
