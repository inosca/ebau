mutation CreateFillFormTask {
  saveCompleteWorkflowFormTask(
    input: { slug: "fill-form", name: "Formular ausfüllen" }
  ) {
    clientMutationId
  }
}

mutation CreateBuildingPermitWorkflow {
  saveWorkflow(
    input: {
      slug: "building-permit"
      name: "Baugesuch"
      startTasks: ["fill-form"]
      allowForms: ["baugesuch"]
    }
  ) {
    clientMutationId
  }
}
