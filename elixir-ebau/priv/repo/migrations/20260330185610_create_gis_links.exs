defmodule Ebau.Repo.Migrations.CreateGisLinks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:gis_links, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :placeholder, :text, null: false

      add :service_id,
          references(:SERVICE,
            column: :SERVICE_ID,
            name: "gis_links_service_id_fkey",
            type: :bigint,
            prefix: "public"
          ),
          null: false
    end
  end

  def down do
    drop constraint(:gis_links, "gis_links_service_id_fkey")

    drop table(:gis_links)
  end
end
