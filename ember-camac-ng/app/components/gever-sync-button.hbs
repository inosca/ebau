<UkButton
  @color="primary"
  @disabled={{this.sync.isRunning}}
  @loading={{this.sync.isRunning}}
  @onClick={{this.sync.perform}}
  @title={{t "gever.sync"}}
  data-test-gever-button
  type="button"
>{{t "gever.sync"}}</UkButton>