<div class="uk-width-1" {{did-insert (perform this.fetchCase)}}>
  {{#if this.isLoading}}
    <div class="uk-flex uk-flex-center">
      <UkSpinner />
    </div>
  {{else}}
    <DashboardSection @sectionTitle={{t "dashboard.generalData"}}>
      <tr>
        <DashboardColumn
          @header={{t "cases.tableHeaders.dossierNr"}}
          @value={{this.models.caseModel.dossierNr}}
        />
        <DashboardColumn
          @header={{t "dashboard.buildingProjectStatus"}}
          @value={{this.models.caseModel.buildingProjectStatus}}
        />
      </tr>
      {{#if this.isService}}
        <tr>
          <DashboardColumn
            @header={{"Frist"}}
            @value={{
              moment-format this.models.activation.deadlineDate "DD.MM.YYYY"
            }}
          />
          {{! Empty column so the spacing is correct}}
          <DashboardColumn />
        </tr>
      {{/if}}
    </DashboardSection>
    <hr />

    <DashboardSection @sectionTitle={{t "dashboard.personalData"}}>
      <tr>
        <DashboardColumn @header={{t "cases.tableHeaders.applicant"}}>
          <DashboardAddress @contact={{this.models.caseModel.applicant}} />
        </DashboardColumn>

        {{#if this.models.caseModel.projectAuthor}}
          <DashboardColumn @header={{t "dashboard.projectAuthor"}}>
            <DashboardAddress
              @contact={{this.models.caseModel.projectAuthor}}
            />
          </DashboardColumn>
        {{else}}
          {{! Empty column so the spacing is correct}}
          <DashboardColumn />
        {{/if}}
      </tr>
      <tr>
        {{#if this.models.caseModel.landowner}}
          <DashboardColumn @header={{t "dashboard.landowner"}}>
            <DashboardAddress
              @contact={{this.models.caseModel.projectAuthor}}
            />
          </DashboardColumn>
          {{! Empty column so the spacing is correct}}
          <DashboardColumn />
        {{/if}}
      </tr>
    </DashboardSection>
    <hr />

    <DashboardSection @sectionTitle={{t "dashboard.intentAndLocation"}}>
      <tr>
        <DashboardColumn
          @header={{t "cases.tableHeaders.municipality"}}
          @value={{this.models.caseModel.municipality}}
        />
        <DashboardColumn
          @header={{t "cases.tableHeaders.street"}}
          @value={{this.models.caseModel.street}}
        />
      </tr>

      <tr>
        <DashboardColumn @header={{t "dashboard.parcels"}}>
          <ul class="uk-list uk-list-disc uk-list-collapse">
            {{#each this.models.caseModel.parcelNumbers as |parcelNumber|}}
              <li>
                <a
                  href="https://egov.ur.ch/teraspur/cgi2TERwww.asp?usr=eigentum&pass=terintra2014&gem=1201&nr={{
                    parcelNumber
                  }}"
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {{parcelNumber}}
                </a>
              </li>
            {{/each}}
          </ul>
        </DashboardColumn>
        <DashboardColumn @header={{t "dashboard.egrid"}}>
          <ul class="uk-list uk-list-disc uk-list-collapse">
            {{#each this.models.caseModel.egridNumbers as |egridNumber|}}
              <li>
                <a
                  href="https://oereb.ur.ch/?egrid={{egridNumber}}"
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {{egridNumber}}
                </a>
              </li>
            {{/each}}
          </ul>
        </DashboardColumn>
      </tr>

      <tr>
        <DashboardColumn
          @header={{t "cases.tableHeaders.intent"}}
          @value={{this.models.caseModel.intent}}
          colspan="3"
        />
      </tr>
    </DashboardSection>
    <hr />

    <DashboardJournal @journalEntries={{this.models.journalEntries}} />
  {{/if}}
</div>
