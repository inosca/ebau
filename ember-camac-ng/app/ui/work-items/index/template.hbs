<WorkItemListWrapper
  @order={{this.order}}
  @responsible={{this.responsible}}
  @type={{this.type}}
  @status={{this.status}}
  @role={{this.role}}
  @username={{this.shoebox.content.username}}
  @application={{this.shoebox.content.application}}
  @serviceId={{this.shoebox.content.serviceId}}
  @setFilter={{this.setFilter}}
/>
