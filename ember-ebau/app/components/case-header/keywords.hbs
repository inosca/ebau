<PowerSelectMultipleWithCreate
  @searchEnabled={{true}}
  @searchField="name"
  @searchMessage={{t "cases.header.searchKeyword"}}
  @loadingMessage={{t "cases.header.loadKeywords"}}
  @noMatchesMessage={{t "cases.header.emptyKeywords"}}
  @search={{perform this.search}}
  @selected={{this.selected}}
  @onCreate={{perform this.create}}
  @buildSuggestion={{this.customSuggestion}}
  @onChange={{this.onChange}}
  as |keyword|
>
  {{keyword.name}}
</PowerSelectMultipleWithCreate>