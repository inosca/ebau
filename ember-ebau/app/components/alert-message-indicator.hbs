{{#if (gt this.count 0)}}
  <a
    href="#"
    title="{{t 'alert-message.title'}}"
    {{on "click" this.showAlertMessages}}
  >
    <UkIcon @icon="warning" class="uk-button-danger uk-icon-button" />
  </a>

  <AlertMessagesModal
    @messages={{this.messages}}
    @visible={{this.showModal}}
    @onHide={{this.hideModal}}
  />
{{/if}}