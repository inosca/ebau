<SubNavigation @instanceId={{@model}} />

{{#if this.isAlexandria}}
  <div class="alexandria">
    {{outlet}}
  </div>
{{else}}
  <div class="uk-container main-content with-sidebar">
    <h2>Dossier {{@model}}</h2>

    {{outlet}}
  </div>
{{/if}}
