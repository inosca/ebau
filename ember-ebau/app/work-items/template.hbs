<WorkItemListWrapper
  @order={{this.order}}
  @responsible={{this.responsible}}
  @type={{this.type}}
  @status={{this.status}}
  @role={{this.role}}
  @task={{this.task}}
  @preset={{this.preset}}
  @username={{session-value "user.username"}}
  @application={{session-value "application"}}
  @serviceId={{session-value "service.id"}}
  @roleId={{session-value "role.id"}}
  @serviceGroupSlug={{session-value "serviceGroup.slug"}}
  @baseRole={{session-value "rolePermission"}}
  @setFilter={{this.setFilter}}
  @useLinkTo={{true}}
  @baseRoute="cases.detail.work-items"
/>