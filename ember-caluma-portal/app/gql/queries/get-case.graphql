query GetCase($caseId: GenericScalar!) {
  allCases(metaValue: { key: "camac-instance-id", value: $caseId }) {
    edges {
      node {
        id
        status
        meta
        document {
          id
        }
      }
    }
  }
}
