query GetMunicipalities {
  allQuestions(filter: [{ slug: "gemeinde" }]) {
    edges {
      node {
        ... on DynamicChoiceQuestion {
          options {
            edges {
              node {
                label
                slug
              }
            }
          }
        }
      }
    }
  }
}
