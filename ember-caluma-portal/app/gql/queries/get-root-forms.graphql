query GetRootForms($slug: String) {
  allForms(
    orderBy: NAME_ASC
    metaValue: [{ key: "is-main-form", value: true }]
    slug: $slug
  ) {
    edges {
      node {
        slug
        name
      }
    }
  }
}
