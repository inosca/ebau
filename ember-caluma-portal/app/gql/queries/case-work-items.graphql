query WorkItems($caseId: ID!) {
  allWorkItems(case: $caseId) {
    edges {
      node {
        id
        status
        case {
          id
          meta
        }
        task {
          id
          slug
        }
      }
    }
  }
}
